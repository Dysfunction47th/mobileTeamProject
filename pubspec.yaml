name: mobile_team_project
description: "번호팅 - 익명 기반 매칭 & 채팅 앱"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  web_socket_channel: ^3.0.1

  # 카카오 SDK
  kakao_flutter_sdk: ^1.9.1
  kakao_flutter_sdk_user: ^1.9.1
  kakao_flutter_sdk_share: ^1.9.1
  kakao_flutter_sdk_talk: ^1.9.1
  # 로그
  logger: ^2.0.2
  # env
  flutter_dotenv: ^6.0.1
  #메일
  mailer: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true


  assets:
     - assets/images/kakao_login_medium_wide.png
     - securityData.env